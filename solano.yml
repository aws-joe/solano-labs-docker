#config/solano.yml
---

system:
  docker: true


environment:
  'DOCKER_APP': 'solano-docker'
  'DEPLOY_DOCKER': 'true'
  'DEPLOY_AWS_ECS': 'false'

hooks:
  pre_setup: chmod -R 755 ./scripts
  post_build: ./scripts/solano-docker-deploy.sh

php:
  version: '5.3'

tests:
 - 'php php-app/test.php'
