system:
  docker: true
  queue: docker


environment:
  'DOCKER_APP': 'solano-docker'
  'DEPLOY_DOCKER': 'true'
  'DEPLOY_AWS_ECS': 'false'

timeout_hook: 120

hooks:
  pre_setup: chmod -R 755 ./scripts
  post_setup: ./scripts/docker-build.sh
  post_build: ./scripts/docker-deploy.sh

php:
  version: '5.3'

tests:
 - 'php php-app/test.php'
 - 'php dockerrun.php $TDDIUM_SESSION_ID'
